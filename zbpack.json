{
    "build_command": "npm install && cd frontend && npm install && CI=false npm run build && cd ../backend && npm install",
    "start_command": "cd backend && npm start",
    "cache_dependencies": true,
    "node_version": "18",
    "env": {
        "PORT": "8080",
        "NODE_ENV": "production"
    }
}